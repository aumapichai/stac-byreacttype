import React, { FC } from "react";
import { alpha, styled } from "@mui/material/styles";
import { Tab, TabProps } from "@mui/material";

export const TabComp = styled(Tab)<TabProps>(({ theme }) => ({}));
